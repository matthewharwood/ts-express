{
  "name": "angular-express-server",
  "version": "0.0.1",
  "description": "An express server for angular static compiling.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc && concurrently --k  \"tsc -w -p src\" \"nodemon dist/index.js\""
  },
  "author": "matthewharwood",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.5.0",
    "express": "^4.15.3",
    "nodemon": "^1.11.0",
    "tslint": "^5.5.0",
    "typescript": "^2.4.2"
  }
}
